{% extends 'base.html.twig' %}

{% block title %}Type{% endblock %}

{% block body %}
    <h1>Type</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type.id }}</td>
            </tr>
            <tr>
                <th>Libellefr</th>
                <td>{{ type.libellefr }}</td>
            </tr>
            <tr>
                <th>Libelleen</th>
                <td>{{ type.libelleen }}</td>
            </tr>
            <tr>
                <th>Libellecn</th>
                <td>{{ type.libellecn }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_index') }}">back to list</a>

    <a href="{{ path('type_edit', {'id': type.id}) }}">edit</a>

    {{ include('type/_delete_form.html.twig') }}
{% endblock %}
