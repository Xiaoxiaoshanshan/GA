{% extends 'base.html.twig' %}

{% block body %}  

 {% include ('partials/visitnavbar.html.twig') %} 

 <!-- table -->
 <table class="table bg-white">
  <div class="container">
        <div class="row">
            <div class="col-md-4">
    <thead>         
    <tr>                
        {#<th>Poster</th>#}
        <th>Titre</th>
        {#<th>Description</th> #}              
        <th>Expositon</th>
        <th>Début</th>
        <th>Fin</th>
        {#<th>Nbvisiters</th> #}
        <th>En savoir plus</th>                               
    </tr>
  </thead>
  <tbody>
        {% for exposition in expositions %}
            <tr>
                {#<td>{{ exposition.poster }}</td>#}
                <td>{{ exposition.titre }}</td>
                {#<td>{{ exposition.description }}</td> #} 
                <td><img width="100px" src="/uploads/img/{{ exposition.poster }}" alt=""></td>               
                <td>{{ exposition.datedebut ? exposition.datedebut|date('d-m-Y') : '' }}</td>
                <td>{{ exposition.datefin ? exposition.datefin|date('d-m-Y') : '' }}</td>
                {#<td>{{ exposition.nbvisiters }}</td>#}
                <td>
                    <a href="{{ path('exposition_show', {'id': exposition.id}) }}" class="btn btn-primary">Voir</a>

                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
<<<<<<< HEAD
     <a class="btn btn-secondary" href="{{ path('exposition_edit', {'id': exposition.id}) }}">edit</a>
    {% endif %}
=======
                        <a href="{{ path('exposition_edit', {'id': exposition.id}) }}" class="btn btn-primary">Editer</a>
                    {% endif %}
>>>>>>> fabcd69694c551dd15d0cb49ccd5b6d377345831
                </td>
            </tr>
        {% else %}
        
            <tr>
                <td colspan="10">no records found</td>
            </tr>
        {% endfor %}
  </tbody>
</div>
</div>
</div>


</table>


 

{#*************************************************************************************************#}
    
    <table class="table">
        <thead>
           
        </thead>

        <tbody>
        
       
        </tbody>
    </table>

    
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
 <a href="{{ path('exposition_new') }}">Create new</a>
       
         {% endif %}

    

    {% include ('partials/footer.html.twig') %} 



{% endblock %}



